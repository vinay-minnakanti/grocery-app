# ServiceAccount for External Secrets to authenticate with AWS
apiVersion: v1
kind: ServiceAccount
metadata:
  name: external-secrets-sa
  namespace: grocery-app
  annotations:
    eks.amazonaws.com/role-arn: arn:aws:iam::YOUR_AWS_ACCOUNT_ID:role/grocery-app-external-secrets-role
